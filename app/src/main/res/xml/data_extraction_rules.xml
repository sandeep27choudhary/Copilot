<?xml version="1.0" encoding="utf-8"?>
<!--
    Nothing is eligible for cloud backup or device transfer. The encrypted database is
    useless without the Keystore key, which never leaves the device it was created on, and
    copying financial data into a backup service is exactly what this app exists to avoid.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
